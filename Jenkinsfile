pipeline{
    agent any
    stages{
        stage("build"){
            steps{
                echo "Jenkins build project"
            }
        }
        stage("test"){
            steps{
                echo "Jenkins test project"
            }
        }
        stage("deploy"){
            steps{
                echo "Jenkins deploy project"
            }
        }
    }
}
